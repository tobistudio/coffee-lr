{
  "version": 2,
  "buildCommand": "yarn install && cd apps/storefront && yarn build",
  "outputDirectory": "apps/storefront/build/client",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
